/* Declaro la funcion

function
el valor de retorno x defecto es undefined
tengo q poner return 
*/



/* TAREAS */

/* IVA yo */


/* function iva (pr){
    return (pr * 0.21)
}

let precio = prompt ("Ingrese el precio")

alert ("El IVA es  $" + iva (precio, 0.21)) 
 */

/* IVA profe */

/* function calculariva (precio)
    return (precio * 0.21) */




    /* HORAS yo */

/* function min (hs){
    return (hs*60)
}

    let horas = prompt ("Ingrese las horas")
    alert(horas + " horas equivalen a " + min (horas, 60) + " minutos")  */

    
    
    /* function min (){
        let horas = prompt ("Ingrese las horas")
        return (horas * 60)
    }
    
        let hs = min()
        alert(" Las horas ingresadas equivalen a " +  hs  + " minutos ")  */



    /* HORAS profe */

/*  function calcularminutos (horas){
    return (horas*60)} */



    /* OBTENER NRO correcto */

    /* function obtenernro(){
        let numero = prompt ("Ingrese un numero")
        while(!numero || isNaN (numero)){
        numero = prompt ("Error!  Debe ingresar un numero")
        
        }
        numero = Number(numero)
        return numero
    }
    
    let num = obtenernro()
    alert ("El numero ingresado es " + num)
 */


    /* STRING PROPERTIES / METHOD
    
    string.lenght (largo del string. TOMA LOS ESPACIOS)
    ej: "pepe". lenght


    string.toLowerCase (metodo de los strings, pasa de mayuscula a minuscula)

    "PEPE". toLowerCase() *PASA A MINUSCULA


    .trin (eliminar espacios simples) es util cuando se pide DNI

    .include (sringbuscado)= verifica si el string buscado esta dentro del string  (devuelve true o flase: un booleano)

    .replaceAll(string a reemplazar, valor nuevo)

    .repeat (string a repetir)

    .toUpperCase () =Pasa de minuscula a mayuscula



    */
    


